services:
  translator:
    build:
      context: .
    container_name: translator-service
    network_mode: host
    environment:
      HOST: "0.0.0.0"
      PORT: "5000"
      OLLAMA_HOST: "${OLLAMA_HOST:-http://127.0.0.1:11434}"
    restart: unless-stopped
